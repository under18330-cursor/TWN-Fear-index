# Full development environment: the base dependencies from pyproject.toml
# plus the `finlab` and `dev` extras. Version specs are kept in sync with
# pyproject.toml; equivalent to `pip install -e .[finlab,dev]`.
#
# pandas 3.x is a breaking release, so it is pinned exactly here rather
# than left to float within the range pyproject.toml allows.

# base
pandas==3.0.5
numpy>=1.24
requests>=2.31

# [finlab] extra -- only needed by data_sources_finlab.py
finlab>=2.0

# [dev] extra
pytest>=7.4
